<% if (!user) { %>
  <link rel="stylesheet" href="/stylesheets/siwg.css">
<% } %>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #015351;">
    <div class="container">
      <a href="/" class="navbar-brand">Freshly</a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navmenu" >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navmenu">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item  px-3">
            <a class="nav-link" href="/"
            <%- title === 'Freshly' ? 'class="nav-link active"' : '' %>
          >Home
        </a>
          </li>
          <li class="nav-item px-3">
            <a class="nav-link" 
            href="/food" <%- title === 'Catalog' ? 'class="active"' : '' %>
          >Catalog</a>
          </li>

          <% if (user) { %>
            <li class="nav-item px-3">
              <a class="nav-link" 
              href="/myfridge" <%- title === 'My Fridge' ? 'class="active"' : '' %>
            >My Fridge</a>
            </li>
            <% } %>

          <li class="nav-item px-3">
          <% if (user) { %>
            <a class="nav-link" href="/auth/logout">Logout
              <!-- <button type="submit">Logout</button> -->
            </a>
          <% } else { %>
            <a href="/auth/google" class="nav-link">Sign-In
              <!-- <button type="submit">Sign-In</button> -->
            </a>
          <% } %>
          </li>

        </ul>


      </div>

    </div>
  </nav>
