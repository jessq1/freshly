<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <div class="container-lg py-3 my-3">
    <div class="row">
<div class="col-lg-8">
  <div class="row">
    <div class="col">
      <div class="card shadow p-3 my-2 bg-body rounded mx-auto" style="width: 25rem;">
        <div style="height: 25rem;">
            <a class="btn btn-outline-primary" href="/myfridge/<%= Profile._id %>/fridge ">Look into my Fridge</a>
        </div>
        <div style="height: 15rem;">
          <a class="btn btn-outline-primary" href="/myfridge/<%= Profile._id %>/freezer ">Look into my Freezer</a>
        </div>
      </div>
    </div>
  
    <div class="col">
      <div class="card shadow p-3 my-2 bg-body rounded  mx-auto" style="width: 15rem;">
        <div id='body' style="height: 25rem;">
          <% Profile.fridgeFood.forEach((food) => { %> 
            <p class="mt-2 mb-0"> <%= food.name +" - "+food.freshness %>% </p>
            <progress  max="100" value='<%= food.freshness %>'></progress>
      
          <% }) %>
        </div>
        <div style="height: 15rem;">
          <% Profile.freezerFood.forEach((food) => { %> 
            <p class="mt-2 mb-0"> <%= food.name +" - "+food.freshness %>% </p>
            <progress  max="100" value='<%= food.freshness %>'></progress>
            <% }) %>
        </div>
      </div>
    </div>

  </div>

</div>

<div class="col-lg-4">
  <div>
    <div class="card shadow p-3 my-2 bg-body rounded">
      <div style="height: 20rem;">
        <h6 class="text-center">My Shopping List</h6>
        <table class="table my-3" id="list">
          <thead >
            <tr>
              <th>ITEM</th>
              <th>FRIDGE TIME</th>
              <th>FREEZER TIME</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% Profile.list[Profile.list.length-1].food.forEach((food) => {  %>
              <tr>
                <td><%= food.name %></td>
              <td><%= (food.fridgeM==0? "": food.fridgeM + " Month ") + (food.fridgeW==0? "": food.fridgeW + " Week ") + (food.fridgeD==0? "": food.fridgeD + " Days")%></td>
              <td><%= (food.freezeM==0? "": food.freezeM + " Month ") + (food.freezeW==0? "": food.freezeW + " Week ") + (food.freezeD==0? "": food.freezeD + " Days")%></td>
              <td>
                <form action="/food/<%= food._id %>/addtolist?_method=DELETE" method="POST">
                  <button class="btn btn-outline-danger">X</button>
                </form>
              </td>
        
            </tr>
            <% }) %>
          </tbody>
        </table>
        
        <a class="btn btn-outline-primary m-3" href="/myfridge/<%= Profile._id %>/list ">Look into my List</a>
      </div>
    </div>
    <!-- <div class="card shadow p-3 mx-auto mt-5 bg-body rounded">
      <div style="height: 15rem;">
        <p class="text-center">Previous Shopping List</p>
      </div>
    </div> -->
  </div>
  
</div>

    </div>

  </div>

</main>

<%- include('../partials/footer') %>
